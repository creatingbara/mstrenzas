export type SiteSettings = {
  whatsapp: string;
  instagram: string;
  zone: string;
  hours: string;
  heroTitle: string;
  heroSubtitle: string;
  bookingPolicy: string;
  whatsappMessage: string;
};
